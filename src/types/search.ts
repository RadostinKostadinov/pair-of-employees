export type projectBasedLongestDuration = {
  employees: number[];
  duration: number;
  project: number;
};

export type employeeBasedLongestDuration = {
  employees: number[];
  duration: number;
  projects: number[];
};
